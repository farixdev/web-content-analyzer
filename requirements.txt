# Required
requests>=2.31
beautifulsoup4>=4.12
PySide6>=6.6
python-docx>=1.1          # writer-ready outline .docx export (Export Brief)

# Recommended (much better main-content extraction; the tool falls back to a
# BeautifulSoup heuristic if this is absent)
trafilatura>=1.8

# Optional - only needed if you put a SOCKS proxy (socks5://...) in the Proxy
# field for country-targeted Google search. HTTP/HTTPS proxies need nothing extra.
# requests[socks]>=2.31
